// root.tsx removed — client routing and HMR logic were tied to react-router and Vite.
// The Next.js App Router will handle routing. This file is intentionally
// simplified to avoid references to react-router or import.meta.hot.

export default function RootPlaceholder() {
  return null;
}
